spring:
  application:
    name: oauth2
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: true
  profiles:
    active: test

server:
  port: 8080


